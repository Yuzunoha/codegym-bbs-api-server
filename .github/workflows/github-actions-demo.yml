name: GitHub Actions Demo
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: pwd
      - run: ls
      - run: ls -la
      #- run: make deploy
      #- run: make deploy-phpunit
